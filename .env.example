HOST=0.0.0.0
PORT=8787
PUBLIC_BASE_URL=http://localhost:8787

# Production database. If MYSQL_HOST is set, the server uses MySQL instead of Supabase/db.json.
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=guangzai
MYSQL_PASSWORD=replace_with_password
MYSQL_DATABASE=guangzai_card_game

# Optional cache. Used for sessions, ranking cache, and fast repeated reads.
REDIS_URL=redis://127.0.0.1:6379
